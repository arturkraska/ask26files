Pliki z repozytorium można skompilować używając komend:

`gcc mem.c -O0`
`g++ struct.c`
`g++ union.c`

Warto zauważyć, że w przypadku pliku `mem.c` wynik działania programu może zależeć od użytych opcji kompilatora.